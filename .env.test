# F1240: Test environment .env
# Separate .env.test file for test credentials
# These values are safe for testing and won't affect production

# Supabase (test project)
NEXT_PUBLIC_SUPABASE_URL=https://ivhfuhxorppptyuofbgq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key-safe-for-tests
SUPABASE_SERVICE_ROLE_KEY=test-service-role-key-safe-for-tests

# Vapi (test mode)
VAPI_API_KEY=test-vapi-key
VAPI_WEBHOOK_SECRET=test-webhook-secret

# Twilio (test credentials)
TWILIO_ACCOUNT_SID=ACtest-account-sid
TWILIO_AUTH_TOKEN=test-auth-token
TWILIO_PHONE_NUMBER=+15555551234

# Cal.com (test mode)
CALCOM_API_KEY=test-calcom-key

# Admin bypass key for tests
ADMIN_API_KEY=test-admin-key-for-testing

# Test-specific flags
NODE_ENV=test
